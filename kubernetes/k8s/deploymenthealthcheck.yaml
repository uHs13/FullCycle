apiVersion: apps/v1
kind: Deployment
metadata:
  name: goserver-healthcheck
  labels:
    app: goserver
spec:
  selector:
    matchLabels:
      app: goserver
  replicas: 1
  template:
    metadata:
      name: "goserver"
      labels:
        app: "goserver"
    spec:
      containers:
      - name: goserver
        image: uhs13/publickuberneteshealthcheck:latest
        livenessProbe:
          httpGet:
            path: /healthz
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
          failureThreshold: 3
          timeoutSeconds: 1
          successThreshold: 1
        envFrom:
          - configMapRef:
              name: goserver-configmap
          - secretRef:
              name: goserver-secret
        volumeMounts:
        - mountPath: "/go/fruits"
          name: fruits
      volumes:
      - name: fruits
        configMap:
          name: configmapfruits
          items:
          - key: fruits
            path: "fruits.txt"
